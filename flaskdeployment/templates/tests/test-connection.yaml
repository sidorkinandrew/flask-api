apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flaskdeployment.fullname" . }}-test-connection"
  labels:
    {{- include "flaskdeployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flaskdeployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
